
@{
    ViewBag.Title = "Dictionary";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">

<body>
    <div class="w3-sidebar w3-bar-block w3-card w3-animate-left" style="display:none" width:10% id="mySidebar">
        <button class="w3-bar-item w3-button w3-large"
                onclick="count()">
            Close &times;
        </button>
        <a href="@Url.Action("AddOne", "Dictionary")" class="w3-bar-item w3-button">1. Add one item to Dictionary</a>
        <a href="@Url.Action("AddMany", "Dictionary")" class="w3-bar-item w3-button">2. Add a Huge List of Items to Dictionary</a>
        <a href="@Url.Action("Display", "Dictionary")" class="w3-bar-item w3-button">3. Display Dictionary</a>
        <a href="@Url.Action("Delete", "Dictionary")" class="w3-bar-item w3-button">4. Delete from Dictionary</a>
        <a href="@Url.Action("Clear", "Dictionary")" class="w3-bar-item w3-button">5. Clear Dictionary</a>
        <a href="@Url.Action("Search", "Dictionary")" class="w3-bar-item w3-button">6. Search Dictionary</a>
        <a href="@Url.Action("Index", "Home")" class="w3-bar-item w3-button">7. Return to Main Menu</a>
    </div>

    <div id="main">

        <div class="w3-teal">
            <button id="openNav" class="w3-button w3-teal w3-xlarge" onclick="count()" align="middle">&#9776; Dictionary</button>
            <div class="w3-container">
            </div>
        </div>
        <h3>@ViewBag.Show</h3>
        @if (ViewBag.MyDictionary != null && ViewBag.DictionaryDisplay == true)
        {
            foreach (var sData in ViewBag.MyDictionary)
            {
                <p>@sData</p>
            }
        }
        
        else
        {

        }

            </div>

    <script>
        function count() {
            if (count.num % 2 === 0) {
                w3_open();
                count.num++;
            } else {
                w3_close();
                count.num++;
            }
        }

        // initialize count number
        count.num = 0;

        function w3_open() {
            document.getElementById("main").style.marginLeft = "25%";
            document.getElementById("mySidebar").style.width = "25%";
            document.getElementById("mySidebar").style.display = "block";
            document.getElementById("openNav").style.display = "inline-block";
        }
        function w3_close() {
            document.getElementById("main").style.marginLeft = "0%";
            document.getElementById("mySidebar").style.display = "none";
            document.getElementById("openNav").style.display = "inline-block";
        }
    </script>



</body>

